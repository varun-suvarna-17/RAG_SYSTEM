def process_query(query: str):
    # Step 1: Retrieve data (dummy for now)
    data = ["Student A - 60%", "Student B - 70%"]

    # Step 2: Build context
    context = f"Query: {query}\nData: {data}"

    # Step 3: Generate response (dummy)
    response = f"Based on data: {data}"

    return response
